// GENERATE RANDOM NUMBERS -eg: Between 1 and 50

Math.random() *50;

// ROUNDING DOWN A RANDOM NUMBER TO NEAREST WHOLE.		

math.floor(math.random() * 50)

// MULTIPLY A RANDOM NUMBER:

console.log(Math.random()*100);

// MAKE THE RESULT OF THE ABOVE A WHOLE NUMBER:

console.log(Math.floor(Math.random()*100));

// returns the smallest integer greater than or equal to a decimal number.

console.log(Math.ceil(43.8));

// checks for whole number (interger);

console.log(Number.isInteger(2017));

// checking length:

'Hello'.length

//  Change to upppercase:

'hello'.toUpperCase()

//VARIABLE MATH

let x = 20;
x -= 5; // Can be written as x = x - 5
console.log(x); // Output: 15

let y = 50;
y *= 2; // Can be written as y = y * 2
console.log(y); // Output: 100

let z = 8;
z /= 2; // Can be written as z = z / 2
console.log(z); // Output: 4

//Other mathematical assignment operators include the increment operator (++) and decrement operator (--).

let a = 10;
a++;
console.log(a); // Output: 11

let b = 20;
b--;
console.log(b); // Output: 19


